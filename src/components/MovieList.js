import React from "react";
import { Link } from "react-router-dom";

const MovieList = () => {
  return (
    <section>
      <h4>map through all movies from api here?</h4>
    </section>
  );
};

export default MovieList;
